
@{
    ViewBag.Title = "Marcas";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<ol class="breadcrumb mb-4 m-4">
    <li class="breadcrumb-item"><a href="@Url.Action("Index", "Home")">Inicio</a></li>
    <li class="breadcrumb-item active">Marcas</li>
</ol>

<div class="card">
    <div class="card-header">

        <i class="fas fa-users me-1"></i> Lista de Marcas
    </div>

    <div class="card-body">
        <div class="row">
            <div class="col-12">
                <button type="button" class="btn btn-success" onclick="OpenModal(null);">Crear Nuevo</button>
            </div>
        </div>

        <hr />
        <div class="alert alert-success" role="alert" id="idMsjSucces">
        </div>

        <table id="grid" class="display cell-border" style="width: 100%">
            <thead>
                <tr>
                    <th>Descripción</th>
                    <th>Activo</th>
                    <th>Editar</th>
                    <th>Eliminar</th>
                </tr>
            </thead>
            <tbody>
            </tbody>
        </table>

    </div>
</div>


<!-- Modal -->
<div class="modal fade" id="FormModal" tabindex="-1" aria-labelledby="exampleModalLabel" aria-hidden="true">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header bg-dark text-white">
                <h5 class="modal-title" id="exampleModalLabel">Marca</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <div class="modal-body g-2">

                <input id="idMarca" type="hidden" value="0" />
                <!--<input id="idFechaRegistro"--> @*type="hidden"*@ <!--/>-->

                <form>
                    <div class="mb-3">
                        <label for="exampleInputEmail1" class="form-label">Descripción</label>
                        <input type="text" class="form-control" id="idDescripcion">
                    </div>

                    <label for="exampleInputEmail1" class="form-label">Activo</label>
                    <select class="form-select" id="idActivo" aria-label="Default select example">
                        <option value="1">SI</option>
                        <option value="0">NO</option>
                    </select>

                    <div class="alert alert-danger mt-3" role="alert" id="idMsjModalError"></div>
                </form>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cerrar</button>
                <button type="button" class="btn btn-primary" onclick="Guardar();">Guardar</button>
            </div>
        </div>
    </div>
</div>

@section scripts{
    <script>
        function OpenLoading() {
            $(".modal-body").LoadingOverlay("show", {
                imageResizeFactor: 2,
                text: "Espere porfavor...",
                size: 14
            });
        }
        function CloseLoading() { $(".modal-body").LoadingOverlay("hide"); }

        $(document).ready(function () {
            $("#idMsjModalError").hide();
            $("#idMsjSucces").hide();

        });

        var filaSeleccionada;
        var gridData;
        gridData = $("#grid").DataTable({
            responsive: true,
            ordering: false,
            "ajax": { // ajax traer todos los valores
                url: '@Url.Action("ListarMarcas", "Marca")',
                type: "GET",
                dataType: "json"
            },
            "columns": [
                { "data": "Descripcion" },
                {
                    "data": "Activo", "render": function (data) {
                        if (data) {
                            return '<span class="badge bg-success">Si</span>'
                        } else {
                            return '<span class="badge bg-danger">No</span>'
                        }
                    }
                },
                {
                    "defaultContent":
                        '<button type="button" class="btn btn-primary btn-sm btn-editar"><i class="fas fa-pen"></i></button>' ,
                        //'<button type="button" class="btn btn-danger btn-sm ms-2 btn-eliminar"><i class="fas fa-trash"></i></button>',
                    "orderable": false,
                    "searcheable": false,
                    "width": "70px"
                },
                {
                    "defaultContent":
                        '<button type="button" class="btn btn-danger btn-sm btn-eliminar"><i class="fas fa-trash"></i></button>',
                    "orderable": false,
                    "searcheable": false,
                    "width": "70px"
                }
            ],
            "language": {
                "lengthMenu": "Mostrar " +
                                    '<select>' +
                                    '<option value="10">10</option>' +
                                    '<option value="25">25</option>' +
                                    '<option value="50">50</option>' +
                                    '<option value="100">100</option>' +
                                    '<option value="-1">All</option>' +
                                    '</select>' +
                                " registros por página",
                "zeroRecords": "Nada encontrado - Disculpa",
                "info": "Mostrando la página _PAGE_ de _PAGES_",
                "infoEmpty": "No records available",
                "infoFiltered": "(filtrado de _MAX_ registros totales)",
                "search": "Buscar:",
                "paginate": {
                    "next": "Siguiente",
                    "previous": "Anterior"
                }
            }
        });

        $("#grid tbody").on('click', '.btn-eliminar', function () {
            $("#idMsjSucces").hide();
            var seleccionado = $(this).closest("tr");
            var json = gridData.row(seleccionado).data();

            swal({
                title: "¿Está seguro de eliminar la Marca?",
                type: "warning",
                showCancelButton: true,
                confirmButtonClass: "btn-primary",
                confirmButtonText: "Si",
                cancelButtonText: "No",
                closeOnConfirm: true
            },
                function () {
                    jQuery.ajax({
                        url: '@Url.Action("EliminarMarca", "Marca")',
                        type: "POST",
                        data: JSON.stringify({ id: json.Id }),
                        dataType: "json",
                        contentType: "application/json; charset-utf-8",
                        success: function (data) {
                            CloseLoading();
                            if (data.resultado) {
                                gridData.row(seleccionado).remove().draw();
                                $("#idMsjSucces").text("La Marca fue eliminada correctamente.");
                                $("#idMsjSucces").show();
                            } else {
                                swal("No se pudo eliminar.", data.mensaje, "error");
                            }
                        },
                        error: function (error) {
                            swal("No se pudo eliminar.", data.mensaje, "error");
                            CloseLoading();
                        },
                        beforeSend: function () {
                            OpenLoading();
                        }
                    });
                });

            console.log(json);
        });

        $("#grid tbody").on('click', '.btn-editar', function () {
            //llamo a la tabla, hace referencia a this porque es su evento
            filaSeleccionada = $(this).closest("tr");
            var json = gridData.row(filaSeleccionada).data();
            OpenModal(json);
        });



        function OpenModal(data) {
            $("#idMsjSucces").hide();
            $("#idMsjModalError").hide();

            if (data == null) { // Nuevo
                $("#idMarca").val(0);
                $("#idDescripcion").val("");
                $("#idActivo").val(1);

            } else { // Editar
                $("#idMarca").val(data.Id);
                $("#idDescripcion").val(data.Descripcion);
                $("#idActivo").val(data.Activo == true ? 1 : 0);
            }
            $("#FormModal").modal("show");
        }

        function ValidarCampos() {
            var descripcion = $("#idDescripcion").val();

            if (descripcion == "" || descripcion == null) {
                $("#idMsjModalError").text("Debe ingresar una descripción. Verifique");
                $("#idMsjModalError").show();
                return false;
            }

            if ((descripcion != "" || descripcion != null) && (descripcion.length < 5 || descripcion.length > 100)) {
                $("#idMsjModalError").text("Debe ingresar una descripción válida mayor a 4 caracteres y menor a 100 caracteres.");
                $("#idMsjModalError").show();
                return false;
            }

            return true;
        }


        function Guardar() {
            if (!ValidarCampos()) { return false; }

            var item = {
                Id: $("#idMarca").val(),
                Descripcion: $("#idDescripcion").val().toUpperCase(),
                FechaRegistro: null,
                Activo: $("#idActivo").val() == 1 ? true : false
            }

            jQuery.ajax({
                url: '@Url.Action("GuardarMarca", "Marca")',
                type: "POST",
                data: JSON.stringify({ obj: item }),
                dataType: "json",
                contentType: "application/json; charset-utf-8",
                success: function (data) {
                    CloseLoading();
                    debugger;
                    if (item.Id == 0) { // NUEVO
                        if (data.resultado != 0) {
                            item.Id = data.resultado;
                            gridData.row.add(item).draw(false);
                            $("#FormModal").modal("hide");
                            $("#idMsjSucces").text(data.mensaje);
                            $("#idMsjSucces").show();
                        } else {
                            $("#idMsjModalError").text(data.mensaje);
                            $("#idMsjModalError").show();
                        }
                    } else { // EDITAR
                        if (data.resultado) {
                            gridData.row(filaSeleccionada).data(item).draw(false);
                            $("#FormModal").modal("hide");
                            $("#idMsjSucces").text(data.mensaje);
                            $("#idMsjSucces").show();
                        } else {
                            $("#idMsjModalError").text(data.mensaje);
                            $("#idMsjModalError").show();
                        }
                    }
                },
                error: function (error) {
                    CloseLoading();
                    $("#idMsjModalError").text("Ocurrió un error.");
                    $("#idMsjModalError").show();
                },
                beforeSend: function () {
                    OpenLoading();
                }
        });


    }


    </script>
}

